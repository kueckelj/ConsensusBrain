% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fns.R
\name{plot_brain_surface_3d}
\alias{plot_brain_surface_3d}
\title{Plot Brain in 3D}
\usage{
plot_brain_surface_3d(
  vdf,
  color_by,
  parcellation = NULL,
  parc_highlight = NULL,
  side_highlight = NULL,
  parc_rm = NULL,
  side_rm = NULL,
  vert_rm = NULL,
  pt_clrsp = "Viridis",
  pt_size = 1.5
)
}
\arguments{
\item{vdf}{A data frame containing vertex coordinates and metadata (e.g., x, y, z, and color_by columns).}

\item{color_by}{A string defining the column to use for coloring vertices.}

\item{parcellation}{A string specifying the parcellation column (optional).}

\item{parc_highlight}{A vector of parcellation labels to highlight (optional).}

\item{side_highlight}{A string specifying the hemisphere to highlight ("left" or "right") (optional).}

\item{parc_rm}{A vector of parcellation labels to remove from the plot (optional).}

\item{side_rm}{A string specifying the hemisphere to remove ("left" or "right") (optional).}

\item{vert_rm}{A vector of vertex IDs to remove from the plot (optional).}

\item{pt_clrsp}{A string specifying the colorscale for numeric variables (default: "Viridis").}

\item{pt_size}{A numeric value specifying the size of the points in the plot (default: 1.5).}
}
\value{
A plotly object containing the 3D scatter plot.
}
\description{
This function creates a 3D scatter plot of brain vertices, allowing users to color points by numeric or categorical variables.
}
\examples{
vdf <- data.frame(
  x = rnorm(100),
  y = rnorm(100),
  z = rnorm(100),
  color_by = sample(c("A", "B", "C"), 100, replace = TRUE)
)
plot_brain_surface_3d(vdf, color_by = "color_by")
}
