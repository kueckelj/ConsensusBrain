% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fns.R
\name{identify_obs_within_radius2D}
\alias{identify_obs_within_radius2D}
\title{Identify Observations Within a 2D Radius}
\usage{
identify_obs_within_radius2D(
  cursor_pos,
  radius,
  interaction_template,
  preselected_ids = character(),
  selection_scope = NULL
)
}
\arguments{
\item{cursor_pos}{A numeric vector of length 2 indicating the cursor position (\code{c(col, row)}).}

\item{radius}{A numeric value specifying the radius within which to select observations.}

\item{interaction_template}{A data frame containing at least the columns \code{id}, \code{col}, \code{row},
\code{hemisphere}, and optionally a label column (specified by \code{selection_scope}).}

\item{preselected_ids}{A character vector of IDs that should be excluded from selection. Defaults to \code{character()}.}

\item{selection_scope}{Optional character string naming the column in \code{interaction_template} used
to constrain the selection to observations with the same label and hemisphere as the observation under the cursor.}
}
\value{
A character vector of \code{id}s from \code{interaction_template} that fall within the specified radius
and match the optional label and hemisphere constraints.
}
\description{
Returns the IDs of all observations in a 2D interaction template that lie within
a specified radius of a cursor position. Optionally restricts the selection to a
specific label and hemisphere based on the observation at the center of the cursor.
}
